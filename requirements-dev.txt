-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
respx>=0.21.0          # mock httpx (notion-client uses httpx under the hood)
freezegun>=1.5.0       # freeze datetime.now() in staleness tests
